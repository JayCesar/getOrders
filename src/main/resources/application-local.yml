spring:
  datasource:
    url: jdbc:mysql:aws://database-1.c8vcuqew0oei.us-east-1.rds.amazonaws.com:3306/db_cb4cards?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: admin
    password: ${DB_PASSWORD:password}
    driver-class-name: software.aws.rds.jdbc.mysql.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
